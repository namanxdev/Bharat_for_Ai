# Backend Configuration
# Copy this file to .env and configure as needed

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Google Gemini Configuration (Optional - for LLM response generation)
# If not provided, the backend will use template-based responses
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here
GEMINI_MODEL=gemini-pro

# Twilio Configuration (Optional - for SMS service)
# If not provided, the backend will return mock SMS responses
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# Session Configuration
SESSION_TIMEOUT_MINUTES=30

# CORS Configuration (for development)
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
